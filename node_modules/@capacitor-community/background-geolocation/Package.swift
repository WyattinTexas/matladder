// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "CapacitorCommunityBackgroundGeolocation",
    platforms: [.iOS(.v12)],
    products: [
        .library(
            name: "CapacitorCommunityBackgroundGeolocation",
            targets: ["BackgroundGeolocationPlugin"]
        )
    ],
    dependencies: [
        .package(
            url: "https://github.com/ionic-team/capacitor-swift-pm.git",
            "7.0.0"..<"9.0.0"   // CARD-RUN 9/23: 1.2.26 pins Capacitor 7; the Swift target builds unchanged on 8 (the app is on 8.4.0)
        )
    ],
    targets: [
        .target(
            name: "BackgroundGeolocationPlugin",
            dependencies: [
                .product(name: "Capacitor", package: "capacitor-swift-pm"),
                .product(name: "Cordova", package: "capacitor-swift-pm")
            ],
            path: "ios/Plugin/Swift"
        )
    ]
)
